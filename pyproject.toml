[project]
name = "bus-factor"
version = "0.1.0"
description = "Calculate bus factor for any GitHub repo"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.3",
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
    "rich>=14.3.3",
]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.3.0",
    "pytest>=9.0.2",
]
